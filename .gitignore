__pycache__/
*.pyc
.venv/
.venv310/
settings.json

# Jekyll / Bundler
_site/
.jekyll-metadata
.jekyll-cache/
.bundle/
vendor/
Gemfile.lock

# staticrypt salt (regenerated on each CI run; must not be committed)
.staticrypt.json

# Claude Code local preview server config
.claude/launch.json
